spring:
  main:
    allow-circular-references: true
  application:
    name: unihelp-backend
  datasource:
    url: jdbc:postgresql://localhost:5432/unihelp
    username: postgres
    password: 913700*Aa@
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

server:
  port: 8080

logging:
  level:
    org.springframework: INFO
    org.hibernate.SQL: DEBUG